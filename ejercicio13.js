const nameFinder = [
  'Peter',
  'Steve',
  'Tony',
  'Natasha',
  'Clint',
  'Logan',
  'Xabier',
  'Bruce',
  'Peggy',
  'Jessica',
  'Marc'
];
function finderName(param) {
  // insert code
}

ejercicio 12

const nameFinder = ( 'Peter', 'Steve', 'Tony', 'Natasha', 'Clint', 'Logan', 'Xabier', 'Bruce', 'Peggy', 'Jessica', 'Marc' ):
function finderName(param, name) {
    for (let i = 0; i < param.length; i++) {
        if (param(i) === name) {
            return ( encontró, verdadero, posición:i) 
        }
        
        return (encontró: false)
    }
    console.log (finderName(nameFinder, 'Tony'));
    console.log (finderName(nameFinder, 'Bernardo'));
