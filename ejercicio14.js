const counterWords = [
  'code',
  'repeat',
  'eat',
  'sleep',
  'code',
  'enjoy',
  'sleep',
  'code',
  'enjoy',
  'upgrade',
  'code'
];
function repeatCounter(param) {
  // insert code
}

/ejercicio 13

const counterWords = ( 'code', 'repeat', 'eat', 'sleep', 'code', 'enjoy', 'sleep', 'code', 'enjoy', 'upgrade', 'code' );
const result = ()
for (let i = 0; i < param.llength; i++) {
    const word = param(I)
    result (word) = (result(word) || 0) + 1;
  }
  return result; 
}
const counter =  repeatCounter(counterWords);
console.log(counter);

